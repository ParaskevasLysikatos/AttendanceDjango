from django.apps import AppConfig

#django front-end application, has both bootstrap front(urls,viewsMain,viewsHrms) and rest api(urlsAPI,viewsAPI)
class IpearancefrontConfig(AppConfig):
    name = 'ipearanceFront'
